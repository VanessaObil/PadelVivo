FROM nginx:alpine

# Copia todo el contenido de la carpeta browser a la raíz de nginx para servir
COPY dist/padelvivo/browser /usr/share/nginx/html

# Exponer el puerto 80 (opcional, pero recomendable)
EXPOSE 80
